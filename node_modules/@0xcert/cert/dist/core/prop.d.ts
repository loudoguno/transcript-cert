import { MerkleValue } from '@0xcert/merkle';
export interface PropProof {
    path: PropPath;
    values: PropValue[];
    nodes: PropNode[];
}
export interface PropValue extends MerkleValue {
}
export interface PropNode {
    index: number;
    hash: string;
}
export declare type PropPath = PropKey[];
export declare type PropKey = string | number;
